version: '3'
services:
  shinobi:
    build:
      context: .
      args:
        SHINOBI_BRANCH: dev
    volumes:
      - /dev/shm/ShinobiRAM:/dev/shm/streams
      - $HOME/ShinobiAddon:/data
    ports:
      - "8080:8080"
    environment:
      - SHINOBI_UPDATE=false
    restart: unless-stopped
